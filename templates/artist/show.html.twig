{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artistes</h1>

    <table class="table">
        <tbody>
        {% if is_granted('ROLE_ADMIN') %}

            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
        {% endif %}
            <tr>
                <th>Nom</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>Nom de scène</th>
                <td>{{ artist.nickname }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button2 b-red little rot-135" href="{{ path('artist_index') }}">retour</a>
    <br>
    {%  if is_granted('ROLE_ADMIN') %}
    <a class="button2 b-red little rot-135" href="{{ path('artist_edit', {'id': artist.id}) }}">modifier</a>
    {% endif %}
    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
