{% extends 'base.html.twig' %}

{% block title %}Location index{% endblock %}

{% block body %}
    <h1>Lieux</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for location in locations %}
            <tr>
                <td>{{ location.id }}</td>
                <td>{{ location.adress }}</td>
                <td>{{ location.date ? location.date|date('d-m-Y') : '' }}</td>
                <td>
                    <a href="{{ path('location_show', {'id': location.id}) }}">voir</a>
                    <a href="{{ path('location_edit', {'id': location.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('location_new') }}">Ajouter un lieu</a>
{% endblock %}
