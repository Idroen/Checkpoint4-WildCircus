<nav class="navbar navbar-expand-lg navbar-light bg-black">
    <a class="navbar-brand  button2 b-red rot-135 flicker neon" href="{{ path('app_index') }}">Wild Circus</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">

                <a class="button2 b-red rot-135" href="{{ path('app_index') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="button2 b-red rot-135" href="{{ path('performance_index') }}">Performances</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="button2 b-red rot-135" href="{{ path('category_index') }}">Catégories</a>
            </li>
            {% endif %}
            <li class="nav-item">
                <a class="button2 b-red rot-135" href="{{ path('artist_index') }}">Artistes</a>
            </li>

            <li class="nav-item">
                <a class="button2 b-red rot-135" href="{{ path('location_index') }}">Lieux</a>
            </li>

            <li class="nav-item">
                <a class="button2 b-red rot-135" href="{{ path('contact_new') }}">Contact</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                {% if app.user %}
                    {{ app.user.email }}
                    <a class="nav-link  text-white" href="{{ path('app_logout') }}"> Se déconnecter</a>
                {% else %}
                    <a class="nav-link  text-white" href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
